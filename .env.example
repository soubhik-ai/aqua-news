# Required for cloud deployment (GCP Secret Manager)
# Not needed for local development — the pipeline falls back to config/feeds.json
GCS_BUCKET=your-gcs-bucket-name
GCS_BLOB=latest.json
FEEDS_SECRET=feeds-config
GCP_PROJECT=your-gcp-project-id
